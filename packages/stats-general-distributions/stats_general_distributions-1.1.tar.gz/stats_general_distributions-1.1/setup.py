from setuptools import setup

setup(name='stats_general_distributions',
      version='1.01',
      description='Gaussian and Binomial distributions',
      packages=['stats_general_distributions'],
      zip_safe=False)
