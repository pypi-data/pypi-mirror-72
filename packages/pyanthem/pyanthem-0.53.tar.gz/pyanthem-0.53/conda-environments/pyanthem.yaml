# pyanthem.yaml

# install:  
# update:  conda env update -f pyanthem.yaml 
# activate: conda activate pyanthem
# jupyter: python -m ipykernel install --user --name pyanthem --display-name "Python (pyanthem)"
# remove: conda remove --name pyanthem --all
# fluidsynth: conda install -c conda-forge/label/gcc7 fluidsynth
# fluidsynth: conda install -c conda-forge fluidsynth

name: pyanthem
dependencies:
  - python=3.7
  - pip
  - jupyter
  - ipykernel # Need to test if this is neccesary
  - git
  - pip:
    - pyanthem
