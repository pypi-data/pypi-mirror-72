from setuptools import setup

setup(name='fk_distributions',
      version='0.1',
      description='Gaussian distributions as well as Binomial Distributions',
      packages=['distributions'],
      zip_safe=False)
