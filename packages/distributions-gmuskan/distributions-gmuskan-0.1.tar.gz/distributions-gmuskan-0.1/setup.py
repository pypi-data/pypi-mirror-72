from setuptools import setup

setup(name='distributions-gmuskan',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions'],
      author = 'Muskan',
      zip_safe=False)
