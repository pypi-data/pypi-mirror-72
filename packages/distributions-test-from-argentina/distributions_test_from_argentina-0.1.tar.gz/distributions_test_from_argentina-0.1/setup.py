from setuptools import setup

setup(name='distributions_test_from_argentina',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_test_from_argentina'],
      zip_safe=False)
