from setuptools import setup

setup(name='bgg_distributions',
      version='0.1',
      description='Binomial, Gaussian and General distributions',
      packages=['bgg_distributions'],
      zip_safe=False)
