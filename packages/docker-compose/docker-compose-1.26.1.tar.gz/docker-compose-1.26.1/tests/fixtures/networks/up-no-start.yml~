version: '3.0'
services:
  app:
    command: ping dependency
    depends_on:
      - dependency
    image: alpine:latest
    network_mode: service:network
  dependency:
    command: tail -f /dev/null
    image: alpine:latest
  network:
  command: tail -f /dev/null
  image: alpine:latest
