<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="account_asset_profile_form" model="ir.ui.view">
    <field name="name">account.asset.profile.form.company</field>
    <field name="model">account.asset.profile</field>
    <field name="inherit_id" ref="account_asset_management.account_asset_profile_view_form" />
    <field name="arch" type="xml">
      <field name="journal_id" position="attributes">
        <attribute name="domain">[('company_id','=',company_id)]</attribute>
      </field>
      <field name="account_asset_id" position="attributes">
        <attribute name="domain">[('company_id','=',company_id)]</attribute>
      </field>
      <field name="account_depreciation_id" position="attributes">
        <attribute name="domain">[('company_id','=',company_id)]</attribute>
      </field>
      <field name="account_expense_depreciation_id" position="attributes">
        <attribute name="domain">[('company_id','=',company_id)]</attribute>
      </field>
      <field name="account_plus_value_id" position="attributes">
        <attribute name="domain">[('company_id','=',company_id)]</attribute>
      </field>
      <field name="account_min_value_id" position="attributes">
        <attribute name="domain">[('company_id','=',company_id)]</attribute>
      </field>
      <field name="account_residual_value_id" position="attributes">
        <attribute name="domain">[('company_id','=',company_id)]</attribute>
      </field>
    </field>
  </record>
</odoo>
