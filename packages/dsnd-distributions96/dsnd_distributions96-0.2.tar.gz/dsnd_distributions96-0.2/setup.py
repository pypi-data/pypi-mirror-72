from setuptools import setup

setup(name='dsnd_distributions96',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['dsnd_distributions96'],
      zip_safe=False)
