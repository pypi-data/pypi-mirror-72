from setuptools import setup

setup(name='TESTING123_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['TESTING123_distributions'],
      zip_safe=False)
