from setuptools import setup

setup(name='nrz_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['nrz_distributions'],
      author = 'Rosalie Zhu',
      zip_safe=False)
