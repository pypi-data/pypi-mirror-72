from setuptools import setup

setup(name='distributions-zoo',
      version='0.1',
      description='Gaussian, Binomial distributions package',
      packages=['distributions'],
      zip_safe=False)
