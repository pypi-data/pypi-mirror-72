from setuptools import setup

setup(name='gubi_distributions',
      version='0.1',
      description='Normal/Gaussian and Binomial distributions',
      packages=['gubi_distributions'],
      author = 'Samson Adetutu',
      zip_safe=False)
