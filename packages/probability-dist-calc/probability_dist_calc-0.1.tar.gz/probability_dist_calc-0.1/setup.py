from setuptools import setup

setup(name='probability_dist_calc',
      version='0.1',
      description='probability distributions calculation',
      packages=['probability_dist_calc'],
      zip_safe=False)
