from setuptools import setup

setup(name='dsnd-probability-distributions-varun',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      zip_safe=False)
