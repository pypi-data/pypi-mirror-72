#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['autoclasstoc']

package_data = \
{'': ['*'], 'autoclasstoc': ['_static/*']}

install_requires = \
['sphinx~=3.0', 'docutils']

extras_require = \
{'docs': ['sphinx>=3.1', 'sphinx_rtd_theme'],
 'tests': ['pytest', 'pytest-cov', 'coveralls']}

setup(name='autoclasstoc',
      version='1.2.0',
      description='Add a succinct TOC to auto-documented classes.',
      author='Kale Kundert',
      author_email='kale@thekunderts.net',
      url='https://github.com/kalekundert/autoclasstoc',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='~=3.6',
     )
