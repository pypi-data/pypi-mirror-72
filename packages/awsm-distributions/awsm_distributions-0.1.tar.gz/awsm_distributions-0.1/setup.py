from setuptools import setup

setup(name='awsm_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['awsm_distributions'],
      author='Paulo Alves',
      zip_safe=False)
