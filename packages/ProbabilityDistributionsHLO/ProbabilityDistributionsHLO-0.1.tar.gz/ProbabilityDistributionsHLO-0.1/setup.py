from setuptools import setup

setup(name='ProbabilityDistributionsHLO',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['ProbabilityDistributionsHLO'],
      zip_safe=False)
