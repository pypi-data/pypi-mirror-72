from setuptools import setup

setup(name='bithi_distributions',
      version='0.2',
      description='Gaussian distributions',
      packages=['bithi_distributions'],
      author='Bithi',
      zip_safe=False)
