from setuptools import setup

setup(name='bgg_distribution',
      version='0.1',
      description='Gaussian distributions',
      packages=['bgg_distribution'],
      author= 'Noe Durocher',
      zip_safe=False)
