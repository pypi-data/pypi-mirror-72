from setuptools import setup

setup(name='dnk_distributions',
      version='1.0',
      description='Gaussian and Binomial distributions',
      packages=['dnk_distributions'],
      zip_safe=False)
