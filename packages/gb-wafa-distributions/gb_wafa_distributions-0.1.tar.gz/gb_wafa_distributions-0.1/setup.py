from setuptools import setup

setup(name='gb_wafa_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gb_wafa_distributions'],
      zip_safe=False)
