from setuptools import setup

setup(name='meo_distributions',
      version='0.1',
      description='Gaussian distribution and Binomial Distribution',
      packages=['meo_distributions'],
      author= 'Moses Egbo',
      zip_safe=False)
