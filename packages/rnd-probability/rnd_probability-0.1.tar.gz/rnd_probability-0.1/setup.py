from setuptools import setup

setup(name='rnd_probability',
      version='0.1',
      description='Gaussian distributions and Binomial distribution',
      packages=['rnd_probability'],
      zip_safe=False)
