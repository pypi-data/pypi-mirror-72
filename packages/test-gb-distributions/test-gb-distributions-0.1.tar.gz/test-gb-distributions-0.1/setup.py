from setuptools import setup

setup(name='test-gb-distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['test-gb-distributions'],
      zip_safe=False)
