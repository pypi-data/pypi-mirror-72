from setuptools import setup

setup(name='dsnd_prob_basic',
      version='0.1',
      description='Gaussian distributions',
      author = 'Ahmed Mohsen',
      packages=['distributions'],
      zip_safe=False)
