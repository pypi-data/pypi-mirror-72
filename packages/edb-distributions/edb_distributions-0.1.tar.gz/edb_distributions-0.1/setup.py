from setuptools import setup

setup(name='edb_distributions',
      version='0.1',
      description='Module for defining and inspecting Gaussian and Binomial distributions',
      packages=['distributions'],
      zip_safe=False)
