from setuptools import setup

setup(name='distributions_test_meriem',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_test_meriem'],
      zip_safe=False)
