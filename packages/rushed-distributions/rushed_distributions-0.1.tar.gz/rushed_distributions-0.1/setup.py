from setuptools import setup

setup(name = 'rushed_distributions',
      version = '0.1',
      description = 'Binomial and Gaussian distributions',
      packages = ['rushed_distributions'],
      author = 'Rushikesh Deotale',
      zip_safe = False)
