from setuptools import setup

setup(name='mem_test_probability',
      version='0.1.1',
      description='Gaussian and Binomial distributions',
      packages=['mem_test_probability'],
      zip_safe=False)
