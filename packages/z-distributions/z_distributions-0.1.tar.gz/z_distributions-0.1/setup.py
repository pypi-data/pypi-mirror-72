from setuptools import setup

setup(name='z_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['z_distributions'],
      author='Chiazokam Echeta',
      zip_safe=False)
